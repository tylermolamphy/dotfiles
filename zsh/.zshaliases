alias ll="ls -las"
alias last="ls -last"
alias "cd.."="cd ../"
alias v="vim"
alias vi="vim"
alias emacs="vim"
alias trace="traceroute"
alias p="ping"
alias g="git"
alias ga="git add"
alias gc="git commit -m"
alias gs="git status"
alias gd="git diff"
alias gf="git fetch"
alias gm="git merge"
alias gr="git rebase"
alias gp="git push"
alias gu="git pull"
alias gco="git checkout"
alias mixer="alsamixer"
alias neofetch="neofetch --color_blocks off"
alias vlc='/Applications/VLC.app/Contents/MacOS/VLC --intf rc'
alias y='yt-dlp -U ; yt-dlp -v'
alias y3='yt-dlp -U ; yt-dlp -v -i --extract-audio --audio-format mp3'
alias r='rm -rfv'
alias tail-nginx='tail -f /var/log/nginx/*.log'
alias grab='sudo apt update && sudo apt install'
alias lastlogins='egrep "Successful|Accepted" /var/log/auth.log'
alias dupdate='docker-compose up --force-recreate --build -d'
alias dps='docker ps --format "{{.Status}}: {{.Names}}"'
alias compose=docker-compose
alias dc=docker-compose
alias d=docker
alias get='sudo yum install'
alias pack='sudo pacman -Syu'
alias packgrep="pacman -Ss"
alias tarup='tar -czf'
alias tardown='tar -xvf'
alias tarpeek='tar -tvf'
alias host='python3 -m http.server --bind 127.0.0.1'
alias gg="git add . && git commit && git push"
alias ports="netstat -anop | grep tcp | grep LISTEN | grep -v zerotier"
alias upcp='sudo apt update && sudo apt upgrade && sudo apt autoremove || sudo dnf update || sudo pacman -Syyu || brew upgrade'
alias updot='pushd ~/dotfiles ; git pull ; sleep 1 ; for stowable in `find ~/dotfiles -type d -maxdepth 1 -mindepth 1 ! -name .git | xargs -n1 basename` ; do stow -Rv $stowable ; done ; ~/.vim/rake.sh ; git status ; popd'

function t() {
  X=$#
  [[ $X -eq 0 ]] || X=X
  tmux new-session -A -s $X
}
